# QuickDL v9

Modern responsive UI refresh for QuickDL. Keeps the existing FastAPI + yt-dlp backend and DATABASE_URL setup. R2 is not required by this build.

Support: costumer@quickdl.site

Deploy the repository to Render using the included Dockerfile/render.yaml. Clear the browser/site cache after deployment if an older stylesheet is still visible.
